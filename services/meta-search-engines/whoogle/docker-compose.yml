version: '3.8'

services:
  whoogle:
    container_name: whoogle
    image: benbusby/whoogle-search:latest
    network_mode: "container:gluetun"
    restart: always
    environment:
      - WHOOGLE_CONFIG_HTTPS=0 # Set to 1 if using a reverse proxy with SSL
      - WHOOGLE_CONFIG_THEME=dark
      - WHOOGLE_CONFIG_SEARCH_LANGUAGE=lang_en
      - WHOOGLE_CONFIG_TOR=0  # Don't use built-in Tor — go through gluetun VPN directly
      - WHOOGLE_DISABLE_HTTP2=1  # HTTP/2 causes intermittent connection resets
      - WHOOGLE_CONFIG_USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
