# Rust build artifacts
**/target/
**/target_*/

# Index data (runtime)
**/index_data/

# Model files (large, downloaded at runtime)
models/

# OS junk
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp

# Secrets — NEVER commit .env files
.env
tmp/

# Runtime data
services/privacy-layer/tor/data/
services/privacy-layer/tor2/data/
services/traefik/acme/
shared-signals/

# Test artifacts
searx_test.json
*.pyc
__pycache__/

# Hermes agent state
.hermes/plans/
docs/plans/
ROADMAP.md

# QA / Oxiverse Testing round scratch (gitignored — keep out of the tracked tree)
# Use tmp/ (already ignored above) for throwaway round artifacts; see tmp/INSTRUCTIONS.md
.hermes-qa/

# ── WORKER SCRATCH (founder convention: 2026-08-29) ──
# Autonomous loop workers (Hermes kanban) MUST dump all dirty/temp/throwaway
# artifacts here — NEVER in the repo root. Files here are gitignored so they
# can NEVER be committed, even by `git add -A`. If a worker needs a scratch
# file, it goes in scratch/ (or tmp/). Root-level _*.py / verify_*.sh are
# banned from the tracked tree.
scratch/
tmp/
_*.py
verify_*.sh
*.tmp
*_tmp.py

# Editor / tool backup files
*.new
*.bak
*.tmp
services/crawler/queue_data/
.pytest_cache/
node_modules/
dist/
*.log
.venv/
build/
